# Nextflix Frontend

High-fidelity streaming UI built in Next.js (App Router) with responsive flows:
- `/` landing page
- `/login` sign in
- `/profiles` profile selection
- `/browse` hero + rails
- `/watch/[id]` player shell

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.